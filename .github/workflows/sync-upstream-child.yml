name: 同步官方与子分支

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:

concurrency:
  group: upstream-child-sync
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    name: 检查并生成同步分支
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: 检出 main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: true

      - name: 执行选择性同步
        id: sync
        env:
          SYNC_BRANCH: automation/upstream-child-sync
        run: bash scripts/sync-upstream-child.sh

      - name: 创建或更新同步 PR
        if: steps.sync.outputs.has_changes == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          SYNC_BRANCH: ${{ steps.sync.outputs.sync_branch }}
          REPORT_FILE: ${{ steps.sync.outputs.report_file }}
        run: |
          set -euo pipefail
          if [[ ! -f "$REPORT_FILE" ]]; then
            echo "同步报告不存在：$REPORT_FILE" >&2
            exit 1
          fi
          existing_pr=$(gh pr list \
            --repo "$GITHUB_REPOSITORY" \
            --state open \
            --base main \
            --head "$SYNC_BRANCH" \
            --json number \
            --jq '.[0].number')
          if [[ -n "$existing_pr" ]]; then
            gh pr edit "$existing_pr" \
              --repo "$GITHUB_REPOSITORY" \
              --title "同步官方 main 与子分支功能" \
              --body-file "$REPORT_FILE"
            echo "已更新同步 PR #$existing_pr"
          else
            gh pr create \
              --repo "$GITHUB_REPOSITORY" \
              --base main \
              --head "$SYNC_BRANCH" \
              --title "同步官方 main 与子分支功能" \
              --body-file "$REPORT_FILE"
          fi
